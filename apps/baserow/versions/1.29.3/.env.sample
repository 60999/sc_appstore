# 容器名称
CONTAINER_NAME=baserow

# 应用访问地址
PANEL_APP_URL=http://localhost

# 数据库配置
DB_USER=baserow
DB_PASSWORD=baserow
DB_NAME=baserow

# HTTP 端口
PANEL_APP_PORT_HTTP=80

# 密钥配置（请修改为随机字符串）
SECRET_KEY=change-this-to-a-random-string
JWT_KEY=change-this-to-a-random-string
